#include <iostream>

int main(int argc, char *argv[]){

    // Question 1 : Drag and drop the correct comparison operator so the expression returns 1 when a and b are equal.
    // Answer :
    int a = 5;
    int b = 5;
    std::cout << "a == b ? : " << (a==b) << std::endl;

    // Question 2 : Drag and drop the correct comparison operator so the expression returns 1 when x is less than y.
    // Answer :
    int x = 8;
    int y = 12;
    std::cout << "x <= y ? : "<< (x < y) << std::endl;

    // Question 3 : which comparison operator is used to check if two values are equal ?
    // Answer :
    // ==

    // Question 4 : What is the result of the following code ?
    // Answer :
    // 1 (true)
    int x4 = 7;
    int y4 = 10;
    std::cout << "(x4 < y4) ? : "<< (x4 < y4) << std::endl;

    // Question 5 : True or False
    // x >= y checks if x is greater than or equal to y.
    // Answer :
    // True
}