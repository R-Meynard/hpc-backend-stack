#include <iostream>

int main (int argc, char *argv[]){

    // Question 1 : Drag and drop the correct type to declare multiple variable in one line
    // Answer :
    int x = 10, y = 10, z = 100;
    std::cout << x + y + z << std::endl;

    // Question 2 : In C++, you can declare multiple variables of the same type in a single line
    // Answer : True

    // Question 3 : which of the following is a correct way to declare multiple integer variables called a,b and c
    // Answer : 
    int a, b, c;

    // Question 4 : what is the output of the following code ?
    int x1 = 5, y1 = 10;
    std::cout << x1 + y1 << std::endl;

    // Question 5 : which of th following statements correctly assigns the same value to two variables ?
    int x2, y2;
    x2 = y2 = 10;
    std::cout << x2 << " " << y2 << std::endl;
    return 0;
}