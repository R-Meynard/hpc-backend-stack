#include <iostream>
#include <string>

int main(){
    // declare a string variable named message and store the text Hello in the string
    std::string message = "Hello";
    std::cout << "message : " << message << std::endl;
    return 0;
}