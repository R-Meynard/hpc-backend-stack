#include <iostream>

int main(){
    // Declare and assign age
    int age = 26;
    // Declare and assign minAge
    int minAge = 18;

    // Print age >= minAge
    std::cout << (age >= minAge) << std::endl;
    return 0;
}