#include <iostream>

int main(){
    // Instructions
    // Inside main(), complete the following steps:
    // 1. Use a for loop, and
    //     * Set i to 1
    //     * Run the loop as long as i is less than or equal to 5
    //     * Increase i by 1 in each loop
    //     * Inside the loop^, print the value of i using cout

    for(int i = 1; i <= 5; i++){
        std::cout << i << " ";
    }
    std::cout << std::endl;
    return 0;
}