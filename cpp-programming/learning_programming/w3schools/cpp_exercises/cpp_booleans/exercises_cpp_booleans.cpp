#include <iostream>
#include <cmath>

int main( int argc, char *argv[]){

    // Question 1 : Drag and drop the correct type and value to store true in isCodingFun.
    // Answer :
    bool isCodingFun = true;
    std::cout << "isCodingFun : " << isCodingFun << std::endl;

    // Question 2 : Drag and drop the correct type and value so the program outputs 0.
    // Answer : 
    bool isSunny = false;
    std::cout << "isSunny : " << isSunny << std::endl;

    //Question 3 : what will be the output of the following code ?
    bool isSunny1= false;
    std::cout << "isSunny1 : " << isSunny1 << std::endl;
    // Answer :
    // 0

    // Question 4 : Fill in the missing parts to prints 1 (for true) and 0 (for false)
    bool isCodingFun1 = true;
    bool isFishTasty1 = false;
    std::cout << "isCodingFun1 : " << isCodingFun1 << std::endl;
    std::cout << "isFishTasty1 : " << isFishTasty1 << std::endl;

    // Question 5 : Fill in the missing parts to print the value 1 (for true)
    int x = 10;
    int y = 9;
    std::cout << " x > y ? : " <<(x > y) << std::endl;

    // Question 6 : True or False
    // A boolean  variable can store numeric values like 5 or 10.
    // Answer :
    // False
    bool isNumericValue = 1;
    std::cout << "isnumericValue : " << isNumericValue << std::endl;

    // Question 7 : if a boolean value is true, what number will be output ?
    // Answer :
    // 1
    return 0;
}