#include <iostream>
#include <cmath>

int main(int argc, char *argv[]){

    // Question 1 : Drag and drop the correct header file name to use the log() function.
    // Answer :
    // #include <cmath>
    std::cout <<"Value of log(2) = " << std::log(2) << std::endl;

    // Question 2 : Drag and drop the correct function to round 3.7 to the nearest integer.
    // Answer :
    std::cout << "Rounding of 3.7 : " << std::round(3.7) <<  std::endl;

    // Question 3 : Which function can be used to find the highest value between two numbers ?
    // Answer :
    // std::max(x,y)
    std::cout << "Max between 7 & 8 : " << std::max(7,8) << std::endl;

    // Question 4 : Use the correct fucntion to print the square root of x.
    // Answer :
    int x = 64;
    std::cout << "The square root of x : " << std::sqrt(x) << std::endl;

    // Question 5 : Use the correct function to round the number 2.6 to its nearest integer.
    // Answer :
    std::cout << "Rounding of 2.6 : " << std::round(2.6) << std::endl;

    // Question 6 : Which header file is needed to use functions like sqrt, round, and log ?
    // Answer :
    // <cmath>
    
    // Question 7 : What will be the output of the following code ?
    std::cout << "Rounding of 3.7 : " << std::round(3.7) << std::endl;

    return 0;
}