#include <iostream>

int main(int argc, char *argv []){

    // Question 1 : Drag and drop the correct operator so the expression returns 1 when x is equal to 5.
    int x = 5;
    std::cout << "x == 5 ? : " << (x == 5) << std::endl;

    // Question 2 : Drag and drop the correct operator so the expression returns 0 when comparing 10 and 15
    std::cout << "10 > 15 ? : " << (10 > 15) << std::endl;

    // Question 3 : What will b ethe output of the folllowing code ?
    int x1 = 5;
    std::cout << "x1 == 5 ? : " << (x1 == 5) << std::endl;

    // Question 4 : True or False :
    // The expression (10 > 15) returns 1 (true).
    // Answer : 
    // False

    // Question 5 : Which operator is used to check if two values are equal ?
    // Answer :
    // ==
    return 0;
}